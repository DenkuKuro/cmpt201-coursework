#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>

int main() {
  char *input = NULL;
  size_t len = 0;
  getline(&input, &len, stdin);

  printf("%s\n", input);

  free(input);
}
