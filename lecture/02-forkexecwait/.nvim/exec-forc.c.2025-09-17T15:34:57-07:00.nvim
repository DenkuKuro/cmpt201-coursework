#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
  pid_t pid = fork();

  if (pid == 0) {
    execlp("/ls", "/ls", "-a", "-l", "'-h");
  } else {
    execlp("/ls", "/ls", "-a");
  }

  printf("%d\n", getpid());
}
