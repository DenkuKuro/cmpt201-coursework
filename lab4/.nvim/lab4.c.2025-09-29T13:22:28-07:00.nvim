#define _ISOC99_SOURCE

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define HEAP_SIZE 256
#define BLOCK_SIZE 128
#define BUF_SIZE 64


struct header {
  unit64_t size;
  struct header *next;
};

int main(void) { 
  void *h = (void *)sbrk(HEAP_SIZE); 
}
