#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define new_heap_size 256

struct header {
  unit64_t size;
  struct header *next;
};

int main(void) { sbrk(new_heap_size); }
